[package]
name = "math"
version = "0.1.0"
edition = "2021"

[features]
default = []

[dependencies]
glam = { version = "0.30.5", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
